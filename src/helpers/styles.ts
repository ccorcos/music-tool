import { CSSProperties } from "react"

export const buttonBackground = "#efefef"
export const buttonBorder = "#767676"

export const buttonStyles: CSSProperties = {
	background: buttonBackground,
	border: `1px solid ${buttonBorder}`,
	borderRadius: 3,
}
